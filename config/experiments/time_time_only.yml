# C4 wrapper with only the time-gap task enabled.
model:
  variant: mstc

detection:
  gnn_training:
    decoder:
      predict_edge_type:
        enabled: false
      time_gap:
        enabled: true
        lambda_time: 0.3
        hidden_dim: 128
        num_classes: 6